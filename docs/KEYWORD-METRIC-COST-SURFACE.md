# Keyword metric cost surface

Acquisition is caller-controlled and injected; it has no expansion, refresh, retry loop, or replay fetch. For this adapter, N canonical queries are one bounded acquisition input; provider task multiplication and monetary cost are unknown unless returned by the existing transport/evidence layer.
