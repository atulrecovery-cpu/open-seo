# Keyword metric semantics

The verified existing DataForSEO integration uses Labs keyword overview or Google Ads search-volume paths. `search_volume` is provider-supplied estimated metric data, not actual searches. Monthly rows are preserved and sorted. CPC is a paid-search signal, not willingness-to-pay proof. `competition_index` is retained only as paid advertiser competition and is never organic SEO difficulty.
