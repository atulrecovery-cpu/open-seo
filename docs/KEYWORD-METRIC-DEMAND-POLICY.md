# Keyword metric demand policy

Only `PRESENT`, nonzero search volume on a SHA-linked canonical observation matching normalized query, language and market qualifies demand. Explicit `ZERO` maps to `NOT_VALIDATED`; missing/null/invalid/failure remain unknown evidence, not zero. CPC, paid competition, SERP presence, PAA, related searches, competitor wording and model inference cannot validate demand.
